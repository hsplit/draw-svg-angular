import { SvgFilterPipe } from './svg-filter.pipe';

describe('SvgFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SvgFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
